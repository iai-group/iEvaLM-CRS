# Containerization of CRSs

This folder contains the Dockerfiles to build images of the implemented CRSs. The images can be built using the following command from the root of the repository:

```bash
docker build -t <username>/<image_name>:<tag> -f <Dockerfile> .
```
